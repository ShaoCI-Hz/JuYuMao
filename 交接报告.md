# 局域猫播放器 (JuYuMao Player) — 项目交接报告

> 生成日期：2026-08-12 ｜ 迁移来源：`D:\RuanJian\KaiFa\Juyumao` ｜ 迁移目标：`D:\RuanJian\Reasonix\XiangMu\JUYUMAO`
> 迁移方式：逐文件复制（保留目录结构 + `.git` 完整提交历史）

---

## 一、项目概述与完成度

**局域猫 (JuYuMao)** 是一款 Android 局域网 SMB 音乐播放器，由原 Cat Player 全面重构而来（v4.0.0 起更名）。直连 NAS 流式播放，同时支持本地音乐扫描。

| 维度 | 状态 |
|------|------|
| 当前版本 | **v4.0.2**（versionCode 7），versionName 4.0.2 |
| 完成度 | 功能完整（播放/队列/均衡器/歌词/搜索/缓存/歌单/收藏/听歌报告/频谱/引导 均落地） |
| UI 迁移 | 第 2 轮迭代（feature/miuix-ui 分支）已完成：Miuix 0.9.3 全 UI 接入 + Monet 主题 |
| GitHub | 仓库 `ShaoCI-Hz/JuYuMao`（main 分支），Release 至 v4.0.2 |
| 待办 | 真机冒烟（Monet 壁纸变色/三模式走查）；miuix-ui 分支合并 main；后续版本发布 |

---

## 二、技术栈版本

| 类别 | 技术 | 版本 |
|------|------|------|
| 语言 | Kotlin | 2.4.0 |
| 构建工具 | Gradle | 9.4.1（wrapper；AGP 9.2.1 要求 ≥9.4.1） |
| Android Gradle 插件 | AGP | 9.2.1（内置 Kotlin 支持，无需 kotlin-android 插件） |
| 注解处理 | KSP | 2.3.11（KSP2，独立版本线） |
| UI 框架 | Jetpack Compose | BOM 2026.06.01（androidx 1.11.4，与 CMP 1.11.1 对齐） |
| UI 设计语言 | **Miuix** | 0.9.3（miuix-ui / squircle / icons / preference） |
| 依赖注入 | Hilt | 2.60.1 |
| 数据库 | Room | 2.8.4（DB version 3） |
| SDK | compileSdk 37 / minSdk 29 / targetSdk 35 | |
| 包名 | `com.hezi.juyumao`（debug 后缀 `.debug`） | |

---

## 三、依赖清单（gradle/libs.versions.toml 全量）

### Compose 系
- `androidx.compose` BOM 2026.06.01（ui / ui-graphics / ui-tooling / ui-tooling-preview / material3 / material-icons-extended / animation / foundation）

### 核心
- `androidx.navigation:navigation-compose:2.8.5`
- `androidx.lifecycle` runtime / runtime-compose / viewmodel-compose 2.8.7
- `com.google.dagger:hilt-android:2.60.1` + `androidx.hilt:hilt-navigation-compose:1.2.0`
- `androidx.room` runtime / ktx / paging / compiler 2.8.4
- `androidx.media3` exoplayer / session / ui / effect 1.5.1
- `org.jetbrains.kotlinx` coroutines-core / android 1.9.0
- `androidx.datastore:datastore-preferences:1.1.1`
- `androidx.paging` runtime / compose 3.3.5
- `io.coil-kt:coil-compose:2.7.0`
- `androidx.palette:palette-ktx:1.0.0`
- `androidx.glance` appwidget / material3 1.1.1
- `androidx.core:core-ktx:1.15.0`、`androidx.activity:activity-compose:1.9.3`

### 网络 / SMB / 音频
- `com.hierynomus:smbj:0.13.0`（排除 org.bouncycastle，统一 jdk18on 1.75）
- `eu.agno3.jcifs:jcifs-ng:2.1.9`（共享枚举）
- `org.jmdns:jmdns:3.6.3`（mDNS 发现）
- `net.jthink:jaudiotagger:3.0.1`（音频标签/歌词提取）

### Miuix（第 2 轮引入）
- `top.yukonga.miuix.kmp:miuix-ui:0.9.3` / `miuix-squircle` / `miuix-icons` / `miuix-preference`
- ⚠️ `miuix-blur` **未引入**（要求 minSdk 33，本项目 minSdk 29）→ 毛玻璃保留自绘

### 已移除
- `accompanist-permissions`（全项目零引用，权限用原生 ActivityResultContracts）

### 插件
`com.android.application:9.2.1`、`org.jetbrains.kotlin.plugin.compose:2.4.0`、`com.google.dagger.hilt.android:2.60.1`、`com.google.devtools.ksp:2.3.11`

---

## 四、运行方法

> Android 项目：无包管理器、无服务器/端口。Gradle 即构建系统。

### 前置环境
| 项 | 说明 |
|----|------|
| JDK | 17（`JAVA_HOME` 指向 JDK 17） |
| Android SDK | 需含 platforms；android-34/35/36/37.0、build-tools 34~37 |
| local.properties | 含 `sdk.dir`（指向 SDK 路径，含空格需转义） |
| SDK 平台 | **android-37.0 必须**（Miuix 0.9.3 要求 compileSdk 37） |

### 构建命令
```
# Debug（本机 SSL 证书问题 → 用系统 Gradle，不用 gradlew）
$env:JAVA_HOME = "D:\RuanJian\Java"
& "D:\RuanJian\Android tool\Gradle\gradle-9.4.1\bin\gradle.bat" assembleDebug
# Release（minify + R8）
& "...\gradle.bat" assembleRelease
```
产物：`app/build/outputs/apk/debug/app-debug.apk` / `release/app-release-unsigned.apk`
- Debug APK 约 91.1 MB（含 Miuix + Compose 1.11）
- Release APK 约 17.7 MB

### 测试
- 无单元/仪器测试；真机验证用 adb 安装 + logcat

### 环境变量 / 敏感项
- **`JAVA_HOME`**：JDK 路径（构建必需）
- **`ANDROID_HOME`**：SDK 路径（构建必需）
- **`.gh_token`**：GitHub PAT（发布用，**已从仓库 gitignore，迁移时未复制**；字段为 GitHub token，勿外泄）
- git push 需 `-c http.sslVerify=false`（本机证书问题）

### 发布（可选）
```
.\release.ps1 -Version x.y.z -ReleaseNotes "说明"   # 更新版本→编译→打包→提交→建 Release
```

---

## 五、素材与数据文件清单

| 素材 | 位置 | 格式 | 说明 | 必需性 |
|------|------|------|------|--------|
| 应用图标 | `app/src/main/res/mipmap-anydpi-v26/` | vector XML | 自适应图标 | 可重新生成 |
| 图标前景/背景 | `res/drawable/ic_launcher_{foreground,background}.xml` | vector | 图标组成 | 可重新生成 |
| Widget 布局 | `res/layout/widget_initial.xml` | XML | 桌面小组件 | 可重新生成 |
| Widget 元数据 | `res/xml/juyumao_widget_info.xml` | XML | 小组件配置 | 可重新生成 |
| 字符串 | `res/values/strings.xml` | XML | 应用名等 | 可重新生成 |
| 主题 | `res/values/themes.xml` | XML | 系统主题 | 可重新生成 |
| 数据库 | 运行时生成 | SQLite (Room) | `juyumao.db` 存于设备 | 运行时必需，非仓库文件 |
| 音频/封面/歌词缓存 | 运行时生成 | 设备 filesDir/cacheDir | 用户数据 | 非仓库文件 |
| 设计 spec | `docs/compose/spec/*.md`（6 份） | Markdown | 迭代规划与交付记录 | **必需保留** |

> 项目无音频/图片/字体等二进制素材（图标全为 vector 可再生成）。所有运行数据（数据库/缓存）在设备端生成，不在仓库。

---

## 六、核心功能与模块职责

### 架构：Clean-ish（MVVM + Hilt + Room），`com.hezi.juyumao`

| 模块 | 路径 | 职责 |
|------|------|------|
| 应用入口 | `JuYuMaoApplication.kt` / `MainActivity.kt` | Hilt 容器、edge-to-edge、setContent |
| UI 根 | `ui/JuYuMaoApp.kt` | 主题/引导门/Scaffold/底部导航/迷你条 |
| 导航 | `ui/navigation/` | NavGraph 路由 + Screen 定义 |
| **播放核心** | `player/` | `PlaybackController`（队列/播放/无缝/淡入淡出/倍速/统计埋点）、`PlaybackQueue`、`PlaybackStateHolder`（状态轮询）、`MusicPlayerService`（前台通知）、`DynamicLoadControl`（HiRes 分档缓冲）、`SmbMediaSource` |
| 音频 | `player/audio/` | `AudioEffectsManager`（均衡器+低音/环绕/响度）、`SpectrumAnalyzer`（FFT 频谱）、`LrcParser` |
| 数据-本地 | `data/local/` | Room（`JuYuMaoDatabase` v3：songs/servers/playlists/playlist_songs）、`LocalMusicScanner`、`MetadataExtractor`/`MetadataBatchProcessor`（多线程批量缓存）、`LyricsManager`（内嵌+外挂歌词）、`CacheManager`（1GB NAS 上限）、`ArtworkCache`、`CryptoHelper`（Keystore AES-GCM）、`SettingsDataStore` |
| 数据-SMB | `data/remote/smb/` | `SmbClient`/`SmbConnectionPool`（8 连接/30min）、`SmbFileScanner`、`JcifsShareEnumerator`（共享枚举）、`ShareDiscovery`/`NetworkScanner`（mDNS+网段）、`SmbStreamSource` |
| 仓库层 | `data/repository/` | Music/Metadata/Settings/Smb/Lyrics 5 个 Repository |
| DI | `di/` | AppModule（DB/DAO/DataStore）、PlayerModule（ExoPlayer+Miuix）、SmbModule |
| 领域模型 | `domain/model/` | Song/Album/Playlist/SmbServer/PlaybackState |
| **UI 页面** | `ui/*/` | home（首页统计/天气/NAS 状态）、browse（全部/本地/NAS/我喜欢/维度）、search、player（Salt 风格播放页）、queue、playlist、statistics（听歌报告）、cache、settings、equalizer、smb（连接）、sleep、onboarding（首次引导）、widget（Glance） |
| UI 组件 | `ui/components/` `ui/player/components/` | MiniPlayerBar、PremiumBottomNavBar、RotatingAlbumArt、PulsingGlow、AnimatedIconButton、PlayerBackground、CoverLyricsPager、SpectrumBars 等 |
| 主题 | `ui/theme/` | `Theme.kt`（MiuixTheme+Monet）、Color/Type/Shape/FormatUtils |

---

## 七、关键设计决策

| 决策 | 说明 |
|------|------|
| **双库 SMB** | jcifs-ng 枚举共享 + smbj 流式播放（smbj 无 listShares） |
| **自动重连** | 启动时对已保存服务器重建连接，3 次重试 + 20s 整体超时；成功后触发批量缓存 |
| **元数据下载上限 8MB** | SMB 头部下载保留原始扩展名（jaudiotagger 按扩展名解析）；2MB 曾截断大封面 |
| **连接池** | 最大 8 连接、30 分钟空闲回收；内存态（重启丢失）→ 启动自动重连 |
| **批量缓存多线程** | 每服务器独立连接 + worker 连接降级（失败丢弃、至少 1 个、全失败跳过下载） |
| **缓存上限 LRU** | NAS 下载总量约 1GB，按 lastModified 删最旧 |
| **封面按需提取** | `isMetadataCached` 只按 albumArtUri；`artworkInFlight` 失败可重试 |
| **HiRes 判定** | `HiRes.kt` 统一：采样率>48kHz 或位深≥24bit 或 dsf/dff；44.1/16 FLAC 不再误标 |
| **isPlaying 以 ExoPlayer 为准** | onIsPlayingChanged 驱动，避免 SMB 缓冲虚报 |
| **DynamicLoadControl** | 继承 DefaultLoadControl（非接口委托——Kotlin 委托不转发 Java default 方法会崩溃），HiRes 2 倍预缓冲 |
| **Miuix 接入（第 2 轮）** | 全盘 MIUI 原生配色 + Monet 动态取色；保留 LocalExtendedColors(hiResGold)；缺件保留 M3 FilterChip/FilledTonalButton/OutlinedButton |
| **工具链** | AGP 9 内置 Kotlin（移除 kotlin-android 插件）；Kotlin 2.4 / Gradle 9.4.1 / KSP2；Room 2.8.4 修复 KSP2 崩溃；Hilt 2.60.1 适配 AGP 9 |
| **权限** | 首次引导集中申请（通知+音频）；启动不弹窗；拒绝不阻塞 |
| **数据库迁移** | v1→v2（元数据列）、v2→v3（playlist_songs）；禁止 destructive fallback |

---

## 八、已知问题与待办 TODO

### 已知问题 / 局限
1. **FFmpeg 扩展未引入**：`media3-exoplayer-ffmpeg` 未发布到任何 Maven（Google 需自编译 NDK so 库，Windows 不支持）→ DSD/APE/WavPack 解码失败时提示跳过（T9.5 兜底），实际可播：FLAC(含24bit)/WAV/MP3/AAC/OGG/OPUS
2. **miuix-blur 未引入**（minSdk 33 > 本项目 29）→ 毛玻璃保持自绘效果
3. **PlayerSlider 拖拽放大动画移除**（Miuix Slider 无 thumb/track 槽）
4. **QueueViewModel 快照式**：播放中队列变化需重进页面刷新
5. **共享元素转场/迷你条展开** 为低优先遗留（现有入场放大动画满足基准）
6. **SYLT 同步歌词未实现**（AudioMetadata.syncedLyrics 字段存在未填充）
7. **onboarding 老用户**：升级安装若标记缺失会被强制引导（设置页可重看/跳过）
8. **README 徽标版本号过旧**（显示 Kotlin 2.1/Compose 2024.12，实际 2.4/2026.06）

### 待办 TODO
- [ ] **真机冒烟**：连接手机验证 Monet 壁纸变色、深/浅/跟随系统三模式、各页面 Miuix 视觉
- [ ] **合并 main**：`feature/miuix-ui` 分支（T1-T7 已完成）合并到 main
- [ ] **发布 v4.0.3**：`release.ps1 -Version 4.0.3 -ReleaseNotes "Miuix UI 迁移..."`（需 .gh_token，`-c http.sslVerify=false` push）
- [ ] 更新 README 徽标版本（Kotlin 2.4 / Compose 2026.06 / Miuix）
- [ ] 共享元素转场（可选，Compose 1.11 SharedTransitionLayout 实验 API）

---

## 九、迁移验证记录

| 验证项 | 结果 |
|--------|------|
| 源文件数（排除 build/.gradle/.kotlin/.gh_token） | 523 |
| 目标文件数 | 523（+3 为 .reasonix 元数据，非项目内容） |
| 源→目标缺失 | **0**（100% 完整） |
| 关键入口文件 | 13/13 到位 |
| 核心源码 hash 抽查（6 文件） | 全部一致 |
| .git 提交历史 | 已复制（390 文件，647.9KB，含全部分支/提交） |
| 排除项 | app/build（494.7MB 产物）、.gradle、.kotlin、.gh_token（敏感） |
